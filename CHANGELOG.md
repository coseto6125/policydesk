# Changelog

## 0.2.0

這一版讓櫃台離開開發機。它原本依賴區網的 llama-server 與本機登入的 codex CLI，
雲端主機兩樣都沒有。

### 檢索

向量通道改走 Cloudflare Workers AI 的 `@cf/baai/bge-m3`，1024 維，與原本的索引同維度。
manifest 記錄索引是哪個 encoder 建的，查詢時對不上就報錯——量化與未量化的 bge-m3
產生的向量不能互換，混用不會出錯只會安靜地答得比較差。

條文邊界修好了：最長的一筆從 420,375 字降到 54,451，超過十萬字的從 40 筆變 0 筆。
索引因此從 23,588 段降到 15,824 段，少掉的是被誤切成上百段的基金表格與說明書附錄。

產品名抽取原本會抓到頁尾、環保標章編號與章節標題，660 筆裡 96 筆不是產品名，
現在要求印刷標題的證據。

### 對話模型

provider 換成 `claude-haiku-4-5`，讀 Claude Code 的訂閱憑證。憑證讀取唯讀，因為
refresh token 單次輪替，在部署端更新會讓本機的登入失效。選型順序是
`POLICYDESK_PROVIDER` → Anthropic 憑證檔 → `OPENAI_API_KEY` → codex CLI。

Anthropic 的 schema 方言不收 `maxItems`。「這個陣列必須是空的」改寫成 `{"type":"null"}`
並在 provider 邊界讀回空陣列，所以「沒有依據就不准引用」這條約束留在 schema 層，
沒有退到執行期。

### 安全

四項，兩項是跨客戶外洩：

- `_broadcast_desk` 把每份案件快照送給所有後台面板，含身分證字號。兩個訪客同時
  開著就會互看到對方的記錄。
- `decide` 接受任意 `case_id` 而沒有所有權檢查，可以退掉別人的件並取回其個資。
- 路由用整份目錄派工，`verify_identity` 在所有階段都叫得出來。
- 承諾偵測只看第一個命中，「我不保證會核准。不過這件一定會賠。」通得過。

### 稽核

`llm_usage.request` 原本只存 `{"scenario": ...}`，一次 16,781 token 的呼叫留下 22 個
字元。現在存這次呼叫被給了哪些工具、回覆可以有哪些欄位，以及 brief 與輸入的
digest 與長度。不存 prompt 原文：那裡面有姓名、身分證字號與保單內容，複製到第二張
沒有保存期限的表上，代價比它補起來的缺口大。

### 介面

回合完成才解除等待。身分核對會先答被打斷的問題、再說明文件進度，而畫面在第一則
之後就解除，於是那段空檔打的字會插到引導前面，看起來像答非所問。

引用的條文被截斷時會標省略號，取代原本沒有任何 prompt 會讀的布林欄位。

### 部署與工具

`docker-compose.oci.yml` 給雲端主機，不掛 1.1 GB 的 ONNX 模型。憑證同步每 30 分鐘
檢查一次，到期時間驅動而不盲推，本機快過期會主動觸發刷新，並拒絕推送不比遠端新的
token。

GitHub Actions 兩個 job：static 跑 pyci 的 syntax 與 imports 加 ruff，test 起一個真的
Postgres 跑全套。

補宣告 `pypdfium2`——它一直靠 `pdfplumber` 間接帶進來，對方哪天換掉，乾淨環境就會在
import 掛掉而已經裝過的機器一切正常。移除沒用到的 `cachebox`。

## 0.1.0

第一版。
